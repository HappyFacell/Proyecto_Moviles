import 'package:flutter/material.dart';

class ActualOrder extends StatelessWidget {
  const ActualOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Orden actual");
  }
}
